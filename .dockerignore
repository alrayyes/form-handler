.git
.github/workflows/ci.yml
README.md
CHANGELOG.md
*_test.go
# goreleaser's output. Two Linux binaries and their archives, which would
# otherwise be shipped to the daemon as build context on every local build.
/dist
# The linters' own dependencies. Nothing in the image is JavaScript.
/node_modules
/.bun-cache
package.json
bun.lock
