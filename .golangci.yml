version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - dupl
    - err113
    - errorlint
    - gocritic
    - gosec
    - makezero
    - misspell
    - modernize
    - nlreturn
    - revive
    - sqlclosecheck
    - testifylint
    - thelper
    - unconvert
    - unparam
    - usestdlibvars
    - whitespace
    - wrapcheck
  settings:
    gocritic:
      enabled-checks:
        - emptyStringTest
    gosec:
      excludes:
        # The SMTP address comes from configuration this service owns, not from
        # a request, and the bridge it talks to may legitimately be plaintext.
        - G402
    testifylint:
      enable-all: true
    wrapcheck:
      extra-ignore-sigs:
        # contact.Undeliverable already wraps err in a *DeliveryError with its
        # own Unwrap, same reason wrapcheck's own defaults skip errors.New(
        # and .Errorf( — this is this project's version of the same thing.
        - .Undeliverable(
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - dupl

formatters:
  enable:
    - gci
    - gofumpt
    - goimports
